apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hipstershop.fullname" . }}-test-connection"
  labels:
    {{- include "hipstershop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hipstershop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
